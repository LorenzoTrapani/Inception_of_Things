apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3
  labels:
    app: app3
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app3
  template:
    metadata:
      labels:
        app: app3
    spec:
      containers:
      - name: app3
        image: lotrapan/flask-app:18
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: app3-service
  namespace: apps
spec:
  selector:
    app: app3
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP
